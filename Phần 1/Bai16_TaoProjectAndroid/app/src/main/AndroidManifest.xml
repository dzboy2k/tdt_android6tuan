<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="topica.edu.vn.bai16_taoprojectandroid">
    <!-- Cấu hình cấp quyền cho phép đọc tin nhắn -->
    <uses-permission android:name="android.permission.READ_SMS"/>
    <!-- Cấu hình cấp quyền cho phép kết nối internet -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bai16_TaoProjectAndroid">
        <activity android:name="topica.edu.vn.bai16_taoprojectandroid.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>